<?xml version="1.0" encoding="UTF-8"?>
<messages xmlns:if="Wt.WTemplate.conditions">
  <message id="template.field">
    <![CDATA[
    ${<if:{1}>}
    <div class="form-group">
      <label for="${id:{1}}" class="control-label col-sm-2" style="text-align: left">
        ${tr:Wt.Auth.{1}}
      </label>
      <div class="col-sm-7">
        ${{1}}
      </div>
      <div class="help-block col-sm-3" style="text-align: right">
        ${{1}-info}
      </div>
    </div>
    ${</if:{1}>}
    ]]>
  </message>

  <message id="template.field2">
    <![CDATA[
    ${<if:{1}>}
    <div class="form-group">
      <label for="${id:{1}}" class="control-label col-sm-2" style="text-align: left">
        ${{2}}
      </label>
      <div class="col-sm-7">
        ${{1}}
      </div>
      <div class="help-block col-sm-3" style="text-align: right">
        ${{1}-info}
      </div>
    </div>
    ${</if:{1}>}
    ]]>
  </message>

  <message id="template.field3">
    <![CDATA[
    ${<if:{1}>}
    <div class="form-group">
      <label for="${id:{1}}" class="control-label col-sm-2" style="text-align: left">
        ${{2}}
      </label>
      <div class="col-sm-7">
        ${{1}}
      </div>
      <div class="help-block col-sm-3" style="text-align: right">
        ${{1}-info} ${{3} class="btn-link"}
      </div>
    </div>
    ${</if:{1}>}
    ]]>
  </message>

  <message id="template.logged-in">
    <div>
      <div class="Wt-auth-logged-in-left">
        ${nav-b-tex1} ${nav-b-tex2}</div>
      <div class="Wt-auth-logged-in-right">
        <b>${user-name} </b>${logout}</div>
    </div>
    <div class="Wt-auth-logged-in-center"><p><br/></p></div>
    <div class="Wt-auth-logged-in-center">
      ${main-view-widget}
    </div>
  </message>

  <message id="template.registration">
     <div class="Wt-form Wt-auth-registration">

      ${password-description}

      <div class="Wt-fields">
        ${<if:user-name>}
        ${block:template.field user-name}
        ${</if:user-name>}

        ${<if:choose-password>}

        ${block:template.field choose-password}
        ${block:template.field repeat-password}
        ${</if:choose-password>}

        ${<if:email>}
        ${block:template.field email}
        ${</if:email>}

      </div>

      ${<if:oauth>}
      <li>
        <br clear="both" />
        ${oauth-description}
        <label>${tr:Wt.Auth.oauth}
          <span class="Wt-info">${tr:Wt.Auth.oauth-info}</span>
        </label>
        ${icons}
      </li>
      ${</if:oauth>}

      <div class="Wt-buttons">
        ${ok-button} ${cancel-button}
      </div>
    </div>
  </message>

  <message id="template.login">
  <div class="container span8 offset2 Wt-auth-login">
        <legend>${tr:Wt.Auth.login-form-title}</legend>
        <p>${tr:Wt.Auth.login-form-info}</p>

        <div class="form-horizontal">
          ${<if:passwords>}
            ${block:template.field2 user-name user-name-label}
            ${block:template.field password}
            ${block:template.field remember-me}
          ${</if:passwords>}

          ${<if:oauth>}
            <div class="control-group">
              <div class="controls">
                <p>Or login using:</p>
                ${icons}
              </div>
            </div>
          ${</if:oauth>}

          <div class="form-actions">
            <p>${login class="btn-primary"}</p>
            <p>${lost-password class="btn-link"} ${sep}
            ${register class="btn-link"}</p>
          </div>
        </div>
      </div>

  </message>
</messages>
